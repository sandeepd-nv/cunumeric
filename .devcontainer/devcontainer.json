// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/javascript-node
{
    "name": "cunumeric",
    // Use the legate.core devcontainer image as a starting point.
    // Note: This image does not have a prebuilt / precached version of legate.core in it.
    "image": "ghcr.io/nv-legate/legate.core.devcontainer"

    // Use 'postCreateCommand' to run commands after the container is created.
    // "postCreateCommand": "bash scripts/devcontainer/devcontainer-post-create.sh"
}
